run command:
--------------------------------------------------------------------------------
1) For the first problem where page rank has to implemented on the test nodes,
use the following command.

python PageRankAlgo.py TestFile

Output:

After running the command, you can see three files generated in the Output folder.
the file names as iteration-1, iteration-10 and iteration-100

---------------------------------------------------------------------------------

2) For the rest of the problem set use the following run command.

python PageRank.py wt2g_inlinks.txt

After running the above command output files for top page rank and inlink along with 
proportion values will be generated in the Output folder.

---------------------------------------------------------------------------------
The assessment can be found in the Assessment.docx with the conclusion.
 
